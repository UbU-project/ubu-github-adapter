# Code Generation

No generated Rust is committed in this Phase 1 scaffold.

Future code generation tasks must preserve the public contract in `CONTRACT.md`,
must not introduce token persistence, and must include fixture round-trip tests
for any generated wire format.
